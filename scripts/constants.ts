import path from 'path';

export const projDir = path.join(__dirname, '..');

export const workingDir = path.join(projDir, 'working');

export const unpackedApp = path.join(projDir, 'release/win-unpacked/template-electron-cra.exe');
