export interface IDotPosition {
    x: number;
    y: number;
}

export interface IRectPosition extends IElementPositionSize {}

export interface ITextWithPosition {
    text: number | string;
    x: number;
    y: number;
}

export interface IOffset {
    x: number;
    y: number;
}
