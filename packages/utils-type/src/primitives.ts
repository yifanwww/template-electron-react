/**
 * A number type representing the number of milliseconds elapsed since Unix Epoch (00:00:00 UTC on 1 Jan 1970).
 */
export type UnixEpochMS = number & {};

/**
 * A number type representing the number of seconds elapsed since Unix Epoch (00:00:00 UTC on 1 Jan 1970).
 */
export type UnixEpochS = number & {};
