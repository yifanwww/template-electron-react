# @tecra-config/stylelint-config

Stylelint configurations for tecra.

## Usage

Add `"extends": "@tecra-config/stylelint-config"` to your Stylelint config file.

For example `.stylelintrc.json`:
```json
{
    "extends": "@tecra-config/stylelint-config"
}
```
